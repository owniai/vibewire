# Route

ALWAYS state the recommendation — fast routing, no deep deliberation, user decides. ALWAYS include the persisted AIM document path if one exists.

Two options — user confirms which, and this session or a new one:

- **go** — clear, no design needed. Invoke `/vibewire:go`.
- **plan** — directional, needs architectural planning. Follow [plan](plan.md).

New session: output copy-ready prompt: `/vibewire:go` or plan, AIM path if persisted, otherwise a tight digest of the settled spec. Never the exploration process.

## Commit

ALWAYS ask whether to commit, with a recommendation — user decides. Recommend yes when this session created or modified files. Commit ONLY those files.

AIM's responsibility ends here.
